# Contracts

Polygon Amoy testnet setup using Hardhat.

- contracts/DigitalIDRegistry.sol: stores hash of off-chain digital IDs, with status
- scripts/deploy.js: deployment script
- hardhat.config.js: network config (fill RPC and private key)

Install:
```
npm install
npx hardhat compile
```
